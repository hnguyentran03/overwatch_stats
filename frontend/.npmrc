# react-scripts@5.0.1 declares a peerOptional typescript@"^3 || ^4", but this
# project uses TypeScript 5 (required by tsconfig's moduleResolution "bundler").
# CRA 5 works fine with TS 5; this lets npm install/ci resolve without ERESOLVE.
legacy-peer-deps=true
